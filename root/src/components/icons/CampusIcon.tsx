import React, { useState } from "react";

import { LocationOn, LocationOnOutlined } from "@mui/icons-material";

export const CampusIcon = () => <LocationOnOutlined />;

export const CampusIconHovered = () => <LocationOn />;
